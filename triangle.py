import os #load the library
import sys

base = input ("Enter a base : ")
height = input ("Enter a base : ")

area = 1/2 * float(base) * float(height);

print ( "Area of triangle area area=%s base=%s height=%s", area, base, height)
